<?xml version="1.0" encoding="utf-8"?>
<GridLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:useDefaultMargins="true"
    android:rowCount="4"
    android:columnCount="2">

    <ImageView
        android:id="@+id/startingLocationIcon"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:src="@drawable/circle_icon"
        android:layout_gravity="center"
        android:layout_row="0"
        android:layout_marginTop="15dp"
        android:layout_column="0"
        app:tint="@color/dark_gray_white"/>

    <View
        android:layout_width="1dp"
        android:layout_height="25dp"
        android:layout_row="1"
        android:layout_gravity="center"
        android:layout_column="0"
        android:background="@color/dark_gray_white"/>

    <ImageView
        android:id="@+id/destinationIcon"
        android:layout_gravity="center"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:src="@drawable/circle_icon"
        android:layout_row="2"
        android:layout_marginBottom="15dp"
        android:layout_column="0"
        app:tint="@color/dark_gray_white"/>

    <LinearLayout
        android:layout_row="0"
        android:layout_rowSpan="3"
        android:layout_column="1"
        android:orientation="vertical"
        android:layout_height="match_parent"
        android:layout_width="match_parent"
        android:layout_gravity="top">
        <EditText
            android:id="@+id/editTextStartingLocation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/dark_gray_white"
            android:hint="@string/starting_location"
            android:background="@drawable/editview_rounded_corners"
            android:autofillHints="starting location"
            android:textColorHint="@color/dark_gray_white"
            android:inputType="textFilter" />
        <EditText
            android:id="@+id/editTextDestination"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="15dp"
            android:hint="@string/where_to"
            android:textColor="@color/dark_gray_white"
            android:textColorHint="@color/dark_gray_white"
            android:background="@drawable/editview_rounded_corners"/>
    </LinearLayout>

</GridLayout>