<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.UberApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/yellowish</item>
        <item name="colorPrimaryVariant">@color/pewter</item>
        <item name="colorOnPrimary">@color/dark_blue</item>
        <item name="android:colorBackground">@color/pewter</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue_gray</item>
        <item name="colorSecondaryVariant">@color/dark_blue</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <!-- popup menu style-->
        <item name="popupMenuBackground">@drawable/driver_options_menu_bg</item>
        <item name="android:popupMenuStyle">@style/popupBgStyle</item>
    </style>

</resources>